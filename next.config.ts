/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    styledComponents: true, // ✅ Aktifkan styled-components
  },
};

module.exports = nextConfig;
